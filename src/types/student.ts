// src/types/student.ts
export interface Student {
    id?: string;
    name: string;
    class: string;
    section: string;
    rollNumber: string;
    // Add other fields as needed (total 12 fields)
    email: string;
    phoneNumber: string;
    address: string;
    dateOfBirth: string;
    gender: string;
    guardianName: string;
    guardianContact: string;
    admissionDate: string;
  }